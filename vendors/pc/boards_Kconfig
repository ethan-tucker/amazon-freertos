config BOARD_linux
        bool "pc linux"

if BOARD_linux
        rsource "boards/linux/Kconfig"
endif

config BOARD_windows
        bool "pc windows"

if BOARD_windows
        rsource "boards/windows/Kconfig"
endif